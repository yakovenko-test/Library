test {
  database {
    mode = "container" # "external" | "container"
    url = "jdbc:postgresql://localhost:5432/testdb"
    driver = "org.postgresql.Driver"
    user = "test"
    password = "test"
  }
}